# Copy to `.env` and fill in. `.env` is gitignored.
# Source with: `set -a; source .env; set +a`

# HuggingFace — needed to download gated Llama-3.1 weights.
HF_TOKEN=hf_xxx_replace_me
HF_HOME=/mnt/lustre/suny/xliu1/InterpCopy/hf_cache

# Optional: Weights & Biases
WANDB_API_KEY=
WANDB_PROJECT=interpcopy
WANDB_ENTITY=
